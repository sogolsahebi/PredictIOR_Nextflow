docker.enabled = true 

docker {
    enabled = true
    fixOwnership = true
    container = 'nextflow-r-env:latest'
}

process {cd 
    withDocker {
        container = 'nextflow-r-env:latest'
    }
}
