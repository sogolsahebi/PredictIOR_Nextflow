// nextflow.config
docker.enabled = true
docker.fixOwnership = true

profiles {
    standard {
        process.container = 'sogolsahebi/nextflow-r-env:latest'
        docker.enabled = true
        process.executor = 'local'
    }
}
