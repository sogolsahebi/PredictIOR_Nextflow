process testPredictioR {
    container 'nextflow-env:latest'
    script:
    """
    Rscript -e "library(PredictioR); print('PredictioR library loaded successfully')"
    """
}

workflow {
    testPredictioR()
}
